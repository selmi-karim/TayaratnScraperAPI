'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _fetchFromApi = require('./internal/fetchFromApi');

var _fetchFromApi2 = _interopRequireDefault(_fetchFromApi);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Fetches paste data for a specific account (email address).
 *
 * @param {string} email the email address to query
 * @returns {Promise} a Promise which resolves to an array of paste objects
 * (or null if no pastes were found), or rejects with an Error
 * @example
 * pasteAccount('foo@bar.com')
 *   .then((data) => {
 *     if (data) {
 *       // ...
 *     } else {
 *       // ...
 *     }
 *   })
 *   .catch((err) => {
 *     // ...
 *   });
 * @alias module:pasteAccount
 */
var pasteAccount = function pasteAccount(email) {
  return (0, _fetchFromApi2.default)('/pasteaccount/' + encodeURIComponent(email));
};

/**
 * A module for retrieving paste data for a specific account (email address).
 *
 * @module pasteAccount
 * @example
 * import { pasteAccount } from 'hibp';
 */
exports.default = pasteAccount;
//# sourceMappingURL=pasteAccount.js.map